# Copy to backend/fulfillment.env (gitignored — holds a real, dynamically
# generated app token, unlike backend.env's static SECRET_KEY=changeme
# placeholder) and fill in real values from the setup steps in
# backend/apps/fulfillment_webhook_receiver/README.md.

SALEOR_API_URL=http://api:8000/graphql/
FULFILLMENT_WEBHOOK_SECRET=
FULFILLMENT_CONNECTOR_APP_TOKEN=
FULFILLMENT_WAREHOUSE_ID=
