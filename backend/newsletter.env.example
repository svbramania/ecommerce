# Copy to backend/newsletter.env (gitignored). Generate a real token
# rather than leaving this blank or guessable — e.g.:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
NEWSLETTER_ADMIN_TOKEN=
