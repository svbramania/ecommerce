# Copy to backend/payment.env (gitignored — same reasoning as
# fulfillment.env.example: PAYMENT_WEBHOOK_SECRET is a real secret this
# tool generates, not a static placeholder).
PAYMENT_WEBHOOK_SECRET=
